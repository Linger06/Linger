# Linger.Excel.EPPlus

`Linger.Excel.EPPlus` provides the EPPlus-based implementation for the Linger Excel helpers.

This package is used as the concrete Excel engine behind the `Linger.Excel` abstractions.